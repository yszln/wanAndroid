<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yszln.mvvmkt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.project.ProjectActivity"
            android:label="项目" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="登录" />
        <activity android:name=".ui.search.SearchActivity" android:label="搜索" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".ui.common.CommonWebActivity"
            android:label="浏览器" />
        <activity
            android:name=".ui.main.mine.activity.MyArticleArticleActivity"
            android:label="我分享的文章" />
        <activity
            android:name=".ui.main.mine.activity.MyCollectionArticleActivity"
            android:label="我收藏的文章" />
        <activity
            android:name=".ui.main.mine.activity.IntegralActivity"
            android:label="我的积分" />
    </application>

</manifest>